t=int(input())
for i in range(t):
    n=int(input())
    a=[int(x) for x in input().split()]
    x=sum(a)/n
    for k in range(n):
        if a[k]==x:
            print(k+1)
            break
    else:
        print('Impossible')
